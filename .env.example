TZ=UTC
PORT=3333
HOST=localhost
APP_URL=http://${HOST}:${PORT}
LOG_LEVEL=info
APP_KEY=
NODE_ENV=development
SESSION_DRIVER=cookie
LIMITER_STORE=database
MAIL_DRIVER=smtp
MAIL_FROM="SAE UFRRJ <nao-responda@sae.ufrrj.br>"
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
# SMTP autenticado (ex.: institucional UFRRJ) — não preencha para Mailpit:
# SMTP_SECURE=false
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_REJECTUNAUTHORIZED=true
# Produção via Resend: MAIL_DRIVER=resend + domínio verificado no Resend
# RESEND_API_KEY=