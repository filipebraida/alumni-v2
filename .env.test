NODE_ENV=test
TZ=UTC
HOST=127.0.0.1
PORT=3334
LOG_LEVEL=warn

# Reusa o APP_KEY do .env (apenas pra rodada de teste local).
APP_KEY=3Hd-iLpvviA_o45_mLQA4FQEUWFnogXX

# Sessão em memória — browserContext.loginAs / setSession dependem disso.
SESSION_DRIVER=memory

# Mail vai pro driver de log durante testes (não tenta abrir SMTP).
MAIL_DRIVER=smtp
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
